name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref to save CI minutes.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint, typecheck, test & build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: DB migrations in sync with schema
        # `db:generate` writes nothing when the committed migrations already
        # match src/lib/db/schema.ts, so a dirty tree means someone changed the
        # schema without regenerating. Runs offline — no database needed.
        run: |
          npm run db:generate
          if [ -n "$(git status --porcelain drizzle/)" ]; then
            echo "::error::drizzle/ is out of date — run 'npm run db:generate' and commit the result."
            git status --porcelain drizzle/
            exit 1
          fi

      - name: Build
        run: npm run build
